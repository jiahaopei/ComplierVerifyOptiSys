package cn.edu.buaa.optimize;

//MisraC
public class MisraOptimize {

    //未使用的代码、声明
    //注释
    //八进制、十六进制转义禁止。三字符词adv
    //函数应有原型声明
    //初始化明确指定数组大小
    //switch至少有一个case
    //stdarg.h、setjmp.h、signal.h、tgmath.h、不应使用stdlib.h中的内存分配释放、atof、atoi、atol、atoll、abort、exit、getenv、system、bsearch、qsort。不应使用date、time函数。
    //预处理

}
